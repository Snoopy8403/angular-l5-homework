import { InfectionTestPipe } from './infection-test.pipe';

describe('InfectionTestPipe', () => {
  it('create an instance', () => {
    const pipe = new InfectionTestPipe();
    expect(pipe).toBeTruthy();
  });
});
